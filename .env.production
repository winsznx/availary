# Production build override. The Worker serves the API and the static app
# from the same origin, so the client should call relative /api paths
# instead of the local dev Worker at localhost:8787.
# No secrets here — this file is committed.
VITE_API_BASE_URL=
